import { createUseStyles } from "react-jss";

export default createUseStyles((theme) => ({
  container: {
    display: "flex",
    alignItems: "center",
    color: "#fff",
  },
  word: {
    fontWeight: "bold",
    fontSize: 64,
    color: "#00316B",
  },
}));
