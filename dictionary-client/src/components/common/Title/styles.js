import { createUseStyles } from "react-jss";

export default createUseStyles((theme) => ({
  title: {
    textAlign: "center",
    fontSize: 32,
    fontWeight: 600,
    color: "#00316B",
    margin: 0,
    marginBottom: 20,
  },
}));
